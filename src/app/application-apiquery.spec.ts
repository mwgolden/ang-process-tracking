import { ApplicationAPIQuery } from './application-apiquery';

describe('ApplicationAPIQuery', () => {
  it('should create an instance', () => {
    expect(new ApplicationAPIQuery()).toBeTruthy();
  });
});
